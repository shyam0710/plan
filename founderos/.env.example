# Copy this file to .env and fill in your key.
# The key is only used at runtime in your browser — never committed to git.
# For production deployments, proxy the Gemini call through your own backend
# so the key is not visible in the built bundle.

VITE_GEMINI_API_KEY=your_gemini_api_key_here
